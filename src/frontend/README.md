# Frontend

This directory contains frontend UI modules organized by area.

Structure:

- login/
- registration/
- super-admin/
- admin/
- data-encoder/

Each area contains a starter `index.tsx`, `styles.module.css`, a small `README.md`, and a `components/` folder for nested components.
