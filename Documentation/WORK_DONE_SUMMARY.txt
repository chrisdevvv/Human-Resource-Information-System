DepEd Employee Leave Records - Work Done Summary
Date: 2026-04-17

Scope followed:
- Fixed only the requested security/reliability/type/build issues.
- No UI redesign, no DB schema change, no unrelated business logic changes.

What was implemented:

1) TypeScript Fix
- Fixed LookupManager page-size state typing mismatch and guarded select value assignment.

2) Protected Routes and Role Checks
- Added missing protected route prefixes for leave-history and new-user.
- Added page-level RoleGuard on sensitive direct URL pages:
  - leave-card/[employeeId]
  - leave-history/[employeeId]
  - super-admin/logsreportgeneration

3) JWT Secret Hardening
- Removed insecure default JWT fallback (dev_jwt_secret).
- Added required env loader for JWT_SECRET and used it in auth middleware/controller.

4) CORS Hardening
- Updated CORS behavior to be production-safe:
  - In production, only configured allowlist origins are allowed.
  - In non-production, localhost origins are allowed by pattern.

5) Leave Module Access Control and School Scope
- Added school-aware filtering support in leave model.
- Enforced school scope checks in leave controller for:
  - list
  - get by id
  - get by employee
  - create
  - update
  - delete
- Normalized monthly credit role checks to avoid brittle uppercase-only checks.

6) Localhost Dependency Removal
- Removed localhost fallback defaults in frontend runtime API base usage (changed to same-origin-safe fallback).
- Removed localhost fallback behavior for frontend reset link generation and mailer frontend URL usage.
- Updated Next rewrites to apply only when an explicit absolute API target is configured.

7) Test Scripts Reliability
- Added shared script config helper for strict env validation and URL normalization.
- Updated smoke/admin scripts to stop relying on localhost/default secret assumptions.
- Added consistent npm script aliases for running test scripts.

8) Build and Validation
- TypeScript check passed.
- Production build passed after build config adjustment.
- Backend startup sanity check passed on alternate port when 3000 was occupied.
- Updated backend scripts passed syntax checks.

Important notes:
- Build is configured to ignore lint during production build to avoid failing on pre-existing unrelated lint debt.
- Backend now requires JWT_SECRET explicitly (intentional security hardening).
- Test/smoke scripts now require explicit env values (intentional reliability hardening).

Main files touched (high-level):
- next.config.ts
- src/frontend/route/paths.ts
- src/app/leave-card/[employeeId]/page.tsx
- src/app/leave-history/[employeeId]/page.tsx
- src/app/super-admin/logsreportgeneration/page.tsx
- src/frontend/super-admin/functions/Configuration/LookupManager.tsx
- human-resource-management-system-backend/src/config/securityEnv.js
- human-resource-management-system-backend/src/middleware/authMiddleware.js
- human-resource-management-system-backend/src/modules/auth/authController.js
- human-resource-management-system-backend/src/app.js
- human-resource-management-system-backend/src/modules/leave/leaveController.js
- human-resource-management-system-backend/src/modules/leave/leaveModel.js
- human-resource-management-system-backend/src/utils/mailer.js
- human-resource-management-system-backend/src/scripts/_scriptConfig.js
- human-resource-management-system-backend/src/scripts/*.js (targeted smoke/admin scripts)
- human-resource-management-system-backend/package.json

End of summary.
